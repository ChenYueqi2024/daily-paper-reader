---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：一种用于世界与动作建模的统一扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:d"]
score: 9.0
evidence: 用于世界和动作建模的统一扩散Transformer
tldr: 当前机器人策略常依赖大型预训练视觉语言模型作为动作骨干，导致模型庞大。本文提出WorldDiT，一种统一扩散Transformer架构，将动作生成与未来帧视觉预测结合，无需预训练的VLM动作骨干。在四个LIBERO仿真套件中，WorldDiT以低于十亿参数达到平均成功率与参数量的帕累托前沿。该工作展示了统一扩散架构在机器人动作与世界建模中的潜力，为缩放研究提供强基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 现有机器人策略依赖大型预训练VLM作为动作骨干导致模型庞大低效，本文旨在探索无需VLM的轻量统一架构。
method: 提出WorldDiT，一个统一扩散Transformer，同时生成连续动作块并预测未来相机帧的归一化RGB块。
result: 在四个LIBERO模拟套件上，WorldDiT以十亿参数以下达到参数量和成功率的帕累托前沿。
conclusion: WorldDiT为未来机器人缩放研究提供了强基线，展示了统一扩散架构的有效性。
---

## 摘要
近来许多机器人策略通过使用大型预训练的视觉-语言模型（VLM）作为动作骨干来追求更强的控制能力。我们提出了WorldDiT，一种统一的扩散变换器架构，它将动作生成与视觉世界建模相结合，且无需大型预训练VLM动作骨干即可实现强劲性能。在训练过程中，单个扩散变换器生成连续的动作块，并根据未来摄像头帧预测归一化的RGB补丁目标。在四个LIBERO模拟套件中，WorldDiT位于报告的总模型参数与平均成功率之间的帕累托前沿上，这些结果为未来的规模化研究提供了强大的亚十亿参数基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

# WorldDiT 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）
- **现状问题**：当前机器人策略广泛采用大型预训练视觉-语言模型（VLM）作为动作骨干，这带来了强大的感知与语言理解能力，但也导致模型参数庞大（几十亿），且难以区分性能提升究竟来自骨干模型规模还是动作设计本身。
- **研究动机**：探索是否可以在**不使用大型预训练VLM**作为动作骨干的情况下，通过**统一扩散Transformer架构**同时学习动作生成与未来视觉预测，从而实现强控制性能，为未来缩放研究提供亚十亿参数基线。
- **核心思想**：将动作生成与世界建模**耦合**在一个共享的扩散Transformer中，训练时预测动作块和未来RGB patch，推理时仅使用动作分支，从而在较小参数规模下达到高成功率。

## 2. 方法论：核心思想、关键技术细节
- **统一扩散Transformer（WorldDiT）**：共享单一DiT骨干，接收多模态上下文（冻结的视觉编码器+文本编码器+可训练的机器人状态编码器），同时预测**连续动作块**（7步）和**未来归一化RGB patch**（来自主相机和腕相机）。
- **训练流程**：
  - 采用**流匹配（Flow Matching）** 框架：从高斯噪声到干净目标的直线路径，网络预测速度场 \( v_\theta \)。
  - 总损失为动作流损失与RGB patch流损失的加权和：\( L_{total} = w_{action} L_{action}^{flow} + w_{rgb} L_{rgb}^{flow} \)。
  - 动作目标：7维连续动作块，RGB patch目标：从未来帧的224×224图像中提取16×16的归一化patch，每相机保留64个，共128个768维向量。
  - 训练窗口包含10步：3步观测上下文 + 7步动作目标 + 1步未来RGB patch目标。
- **推理流程**：
  - 仅使用动作路径：从高斯噪声初始化，通过20步欧拉积分生成动作块，执行前3个动作后重新规划（滑动窗口+时序集成）。
  - RGB patch预测仅在训练时提供辅助监督，推理时**完全移除**。
- **关键设计**：冻结MAE图像编码器+CLIP文本编码器，可训练机器人状态编码器，统一骨干深度4、隐藏层1024、16头注意力、4个register token。

## 3. 实验设计：数据集、场景、对比方法
- **基准场景**：**LIBERO仿真套件**（包含四个下游套件：Spatial、Object、Goal、Long），另有多任务预训练集libero_90。
- **评估指标**：每个套件500次模拟episode的成功率，平均四个套件得到均值。
- **对比方法**：24种方法，分为两类：
  - **使用大型预训练VLM作为动作骨干**：如ACoT VLA (98.5%), MMaDA VLA (98.0%), VLANeXt (97.4%), OpenVLA OFT (97.1%), π0.5 (96.9%) 等，参数大多在十亿以上。
  - **不使用大型预训练VLM**：如DreamVLA (92.6%), FlowVLA (88.1%), DiT Policy (82.4%), Diffusion Policy (72.4%) 等。
- **WorldDiT结果**：Spatial 98.0%, Object 97.0%, Goal 92.8%, Long 91.8%，均值**94.9%**。总参数量**399.084M**（含冻结模块），可训练参数135.107M。
- **帕累托前沿**：在总参数与平均成功率的散点图中，WorldDiT位于帕累托前沿上——更高成功的所有方法参数都更多，参数更少或相等的方法成功率均更低。

## 4. 资源与算力
- **训练硬件**：单节点配备**8块RTX Pro 6000 GPU**（未说明显存，但RTX Pro 6000为48GB），使用bf16混合精度。
- **预训练**：在libero_90上30个epoch，每GPU batch size 40，梯度累积2，总有效batch size 未明确。
- **微调**：每个下游套件独立微调，有效batch size 512（每GPU batch size 32，梯度累积2，8 GPU）。
- **训练时长**：文中未明确给出具体小时数或天数。
- **推理**：headless EGL渲染，20步Euler采样。

## 5. 实验数量与充分性
- **实验数量**：一个WorldDiT配置，分别在四个套件上微调，每个套件500次episode（共2000次）。其中300次/套件用于阶段性checkpoint选择，因此报告均值94.9%**不是完全独立测试集**，存在一定偏差风险。
- **消融实验**：**无**。论文未做任何消融（如去掉RGB patch目标、改变损失权重、不同骨干深度等）。
- **充分性与公平性**：
  - 对比方法使用了各自论文报告的最佳分数，并未在统一协议下复现，因此存在报告偏差。
  - WorldDiT使用了时序集成和动作前缀执行（执行3/7步），而对比方法可能设定不同，公平性存疑。
  - 未提供多次随机种子实验或置信区间，仅单次500episode结果。
- **总体**：实验设计较为简单，仅验证了单一配置在LIBERO上的可行性，未充分探索设计空间或泛化性。

## 6. 主要结论与发现
- **核心结论**：WorldDiT表明，一个**无需大型预训练VLM的统一扩散Transformer**能够同时学习动作生成与未来视觉预测，并在LIBERO上达到**参数量-成功率帕累托前沿**。
- 在<1B参数范围内，WorldDiT的94.9%均值超越了所有参数更少的方法（如Diffusion Policy 72.4%），且与许多数十亿参数的大VLA方法（如π0.5 96.9%）差距不大。
- **长期套件（Long）**仍是最大挑战（91.8%），与多阶段行为需求一致。
- 该工作为未来机器人策略的缩放研究提供了强基线，支持统一世界-动作建模的紧凑架构方向。

## 7. 优点
- **架构创新**：将动作生成与未来视觉预测**联合训练**但推理时去掉视觉分支，实现了训练时的强监督与推理时的轻量。
- **参数效率**：399M总参数即达到94.9%成功率，显著优于同等参数规模的纯动作扩散策略，展示了耦合世界模型的收益。
- **清晰的问题定位**：明确不依赖大型VLM骨干，有助于解耦架构、数据和模型规模的影响。
- **实验基准价值**：提供了在LIBERO上的公开可复现基线（虽然代码未完全开源），如论文中所述“为缩放研究提供基线”。

## 8. 不足与局限
- **实验覆盖不足**：仅在**LIBERO仿真**上评估，且只有四个同源套件，未涉及真实机器人、多样化环境或不同任务类型（如移动操作、灵巧手）。
- **缺乏消融与鲁棒性分析**：未验证RGB patch预测的贡献、不同损失权重的影响、骨干深度/宽度缩放、采样步数等；未报告多次运行的标准差，单次500episode缺乏统计严谨性。
- **评估偏差风险**：300/500 episodes用于checkpoint选择，报告均值偏高；对比方法使用原始论文分数而非统一复现，比较可能不公平。
- **应用限制**：模型设计依赖冻结的MAE/CLIP编码器，未来对不同视觉域可能需重新训练；辅助RGB patch预测的计算开销在训练时较大（128个patch token），但推理时节省。
- **通用性疑问**：世界建模目标仅预测**单帧未来**的归一化patch，而非完整视频或长期动态，可能不足以支持复杂物理交互推理。
- **未披露完整代码与权重**：降低了可复现性。

（完）
