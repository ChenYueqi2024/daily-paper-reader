---
title: "StereoSplat+: Feed-Forward Stereo Gaussian Splatting with Diffusion-Assisted Progressive Inference"
title_zh: StereoSplat+：基于扩散辅助渐进推理的前馈式立体高斯泼溅
authors: "Zihua Liu, Masatoshi Okutomi"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08808v1"
tags: ["query:d"]
score: 9.0
evidence: 扩散辅助的立体高斯泼溅3D重建
tldr: 从单个立体对重建高质量3D高斯泼溅场景极具挑战，因为存在遮挡和视野限制。本文提出StereoSplat+，其核心是StereoSplat前馈估计器，可处理任意数量立体对并融合代价体积与三分支体积信息。通过扩散增强渐进推理，从单个立体对开始迭代渲染新视图、一步扩散增强并反馈更新高斯。在KITTI-360上，该方法在强外推和遮挡区域显著提升渲染质量与几何精度，超越了现有前馈3DGS基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08808v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 401, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1829, \"height\": 813, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 251, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1713, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1722, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 879, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 875, \"height\": 523, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08808v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1741, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08808v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 869, \"height\": 377, \"label\": \"Table\"}]"
motivation: 现有3DGS依赖多视角或未来帧，无法用于仅单次立体观测的机器人/AR场景。
method: 提出StereoSplat前馈高斯估计器，融合代价体积和三分支体积；设计扩散增强渐进推理，从单立体对迭代渲染、增强和更新。
result: 在KITTI-360上，新视图渲染和几何精度显著提升，尤其在遮挡区域和强外推场景，优于现有前馈3DGS方法。
conclusion: 实现从单次立体观测的因果高质量3DGS重建，为受限感知场景提供可行方案。
---

## 摘要
三维高斯泼溅（3DGS）的最新进展使得高质量、可立即渲染的场景表示成为可能，用于新视角合成。然而，大多数现有的3DGS管线依赖多视角观测（或对未来帧的非因果访问）来实现充分的覆盖，这在设备端机器人技术和增强现实设置中通常不可用，因为这些场景中的传感仅限于单个立体装置。因此，从单一立体观测恢复高质量的3DGS场景仍然具有挑战性，原因在于遮挡、有限的视野和缺失的几何结构。我们提出StereoSplat+，一个扩散增强的前馈框架，能够从单个立体对实现因果重建。我们的方法基于两个关键组件。首先，我们提出StereoSplat，一个输入不变的前馈三维高斯估计器，它接受可变数量的带有位姿的立体对作为输入，并预测高质量的三维高斯体。StereoSplat通过代价体积分支和三平面三维体积分支融合互补的几何线索，并利用连续位姿编码以在视角数量和相机配置之间泛化。其次，由于推理时通常无法获得多个带有位姿的立体对，我们引入一种扩散增强的单次渐进推理方案，称为StereoSplat+：从一个立体对开始，我们从预测的3DGS渲染新的立体视图，使用一步扩散增强器对其进行细化，然后将它们作为额外输入反馈以更新3DGS。在KITTI-360数据集上的实验表明，StereoSplat+提高了新视角渲染质量和几何精度，特别是在遮挡区域和强视角外推下，优于近期的前馈3DGS基线。

## Abstract
Recent advances in 3D Gaussian Splatting (3DGS) have enabled high-quality, render-ready scene representations for novel-view synthesis. However, most existing 3DGS pipelines rely on multi-view observations (or non-causal access to future frames) to achieve sufficient coverage, which is often unavailable in on-device robotics and AR settings where sensing is restricted to a single stereo rig. Recovering a high-quality 3DGS scene from one stereo observation, therefore, remains challenging due to occlusions, limited field of view, and missing geometry. We present StereoSplat+, a diffusion-enhanced feed-forward framework that enables causal reconstruction from a single stereo pair. Our method builds on two key components. First, we propose StereoSplat, an input-invariant feed-forward 3D Gaussian estimator that takes a variable number of posed stereo pairs as input and predicts high-quality 3D Gaussians. StereoSplat fuses complementary geometry cues via a cost-volume branch and a triplane-based 3D volume branch and leverages continuous pose encoding to generalize across view counts and camera configurations. Second, since multiple posed stereo pairs are typically unavailable at inference time, we introduce a diffusion-enhanced one-shot progressive inference scheme called StereoSplat+: starting from one stereo pair, we render novel stereo views from the predicted 3DGS, refine them with a one-step diffusion enhancer, and feed them back as additional inputs to update the 3DGS. Experiments on the KITTI-360 dataset show that StereoSplat+ improves novel-view rendering quality and geometry accuracy, especially in occluded regions and under strong view extrapolation, outperforming recent feed-forward 3DGS baselines.

---

## 论文详细总结（自动生成）

# StereoSplat+: Feed-Forward Stereo Gaussian Splatting with Diffusion-Assisted Progressive Inference - 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：现有前馈式3D高斯泼溅（3DGS）方法通常依赖多视角观测（如多个立体对或非因果访问未来帧）来获得足够的场景覆盖，这在设备端机器人、增强现实等实时、因果推理场景中不可行——传感通常仅限制为单个立体相机。从单个立体对重建高质量3DGS面临着遮挡、视野有限、远距离表面几何缺失等挑战。
- **背景**：前馈3DGS方法（如PixelSplat、MVSplat、DepthSplat、OmniScene）能够从少量已标定图像快速预测3D高斯体，但面对单次立体观测时性能显著下降。扩散模型已被用于引导或修复3DGS重建，但现有方案往往需要逐场景优化或多次采样。
- **核心目标**：提出一种从**单个立体对**进行**因果、前馈式**高质量3DGS重建的框架，同时保持实时性与通用性。

## 2. 论文提出的方法论

### 2.1 整体架构

- **StereoSplat**：输入不变的前馈3D高斯估计器，可接受可变数量（1到K个）的标定立体对，输出高质量3D高斯体。
- **StereoSplat+**：扩散增强的渐进推理方案，从单个立体对开始，通过“渲染→增强→重新注入”的**单轮**循环，自我增强。

### 2.2 关键技术细节

#### (a) StereoSplat 网络结构（图2）
- **特征提取**：使用预训练的 Depth-Anything V2 提取几何特征，CNN提取图像特征，经多视图Transformer（Swin Transformer）聚合左右视图。
- **双分支设计**：
  - **代价体积分支（Cost-Volume Branch）**：基于DepthSplat构建，针对每个参考视图选择最近的N个视图构建相关性代价体积，预测深度偏移和3D高斯参数（中心、不透明度、颜色、尺度、旋转、置信度）。
  - **3D体积分支（Triplane Transformer Branch）**：使用三平面表示（H×W、H×Z、W×Z），通过交叉图像变形注意力和跨平面变形注意力在三维空间聚合证据，预测每个体系的高斯参数。
  - **融合**：将代价体积分支预测的高斯位置投影到三平面，线性映射后添加到平面查询，最终融合两分支的高斯集合。
- **输入不变性**：
  - **连续正弦位姿编码**：对相机到世界坐标准换矩阵进行归一化和正弦编码（包含位置和旋转），取代固定大小的可学习嵌入。
  - **随机视图子采样训练**：在每个训练迭代中，从时间窗口内随机采样数量1~K的立体对作为输入，使模型对输入数量和相机布局不敏感。
- **联合输出**：深度、3D高斯参数、置信度。

#### (b) 渐进推理 StereoSplat+（图1）
- 步骤：
  1. 输入单个立体对 → StereoSplat 预测初始3DGS (G⁰)。
  2. 渲染新立体视图（伪视图）。
  3. 使用**一步扩散增强器**（基于SD-Turbo微调的DIFIX3D+变体）细化伪视图，抑制伪影并提升结构一致性。
  4. 将增强后的伪视图作为额外输入，重新运行StereoSplat预测更新后的3DGS (G¹)。
  5. 对G⁰和G¹的渲染结果进行**置信度引导融合**得到最终输出。
- **伪视图混合训练**：训练时以概率p将采样的真实立体对替换为渲染的伪立体对，缩小训练-推理分布差距。

#### (c) 损失函数
- 重建损失：L₂ + LPIPS
- 深度损失：预测深度与稀疏LiDAR加伪密集深度之间的掩码L₁
- 渲染深度损失：渲染深度与目标深度之间的掩码L₁
- 置信度损失：自监督回归损失（基于解耦光度误差）
- 总损失为各分支（代价体积、3D体积、融合）上述损失的加权和。

## 3. 实验设计

### 3.1 数据集与基准

- **数据集**：KITTI-360（城市驾驶基准），包含校正立体图像、稀疏LiDAR、IMU/GPS位姿。
- **预处理**：沿车辆轨迹划分为时间区间（bin），区间内首个立体对为输入，其余立体对（中心帧、最后帧）为新视角目标。每个bin覆盖约8米距离。
- **数据划分**：共54,862个bin，按9:1分为49,377训练bin和5,485评估bin（排除动态场景严重的序列0003、0007、0010）。
- **评估指标**：
  - RGB：PSNR、SSIM（越高越好）
  - 深度：AbsRel、SqRel（越低越好）

### 3.2 对比方法
- **基线**：PixelSplat、MVSplat、OmniScene、DepthSplat（均为当前顶级前馈3DGS方法）。
- 所有方法均使用**第一个立体对**作为唯一输入。

### 3.3 实验设置
- 分辨率：112×544
- 训练迭代：1,450,000步
- 代价体积：1/4分辨率，192个视差候选
- 三平面网格：192×192×16，嵌入维度128，8头注意力，3层Transformer
- 扩散增强器：微调DIFIX3D+的一步SD-Turbo

## 4. 资源与算力

- **硬件**：4块 NVIDIA A6000 GPU（每块48GB显存）
- **优化器**：AdamW (β=0.9,0.99)，初始学习率8e-5，warmup 1k步
- **训练时长**：论文未明确总训练小时数，但提及“训练30个epoch，批次大小4”，并启用了梯度检查点以节省显存。
- **扩散增强器**：使用预训练模型微调，具体算力未报告。

## 5. 实验数量与充分性

### 5.1 主要实验
- **表1 - 消融研究**：对比代价体积分支、3D体积分支、随机子采样、扩散增强（8种配置），在序列0000的子集（9,207训练/1,023评估bin）上评估，覆盖首帧、中心帧、最后帧及bin平均。
- **表2 - 与基线对比**：在完整验证集（5,485 bin）上定量对比5种方法（含本文StereoSplat和StereoSplat+）。
- **图4**：定性消融比较（新视图渲染，突出遮挡区域）。
- **图5**：定性对比基线（新视图RGB和深度）。
- **图6**：鸟瞰图外推定性比较。
- **图7**：增量大规模重建演示。

### 5.2 充分性与公平性
- **充分性**：覆盖了组件消融、全量基线对比、多帧位置评估、极端外推场景。但仅在KITTI-360单个数据集上验证，缺少跨数据集泛化实验。
- **公平性**：所有基线使用公开代码和默认参数在同一协议下运行，输入条件一致。但需注意基线方法原本基于多视图设计，本文在单视图场景下对比可能不公平（但对本文有利），作者在正文中承认单视图对基线更具挑战性。

## 6. 论文的主要结论与发现

- 在单立体对输入下，StereoSplat（无扩散）在PSNR/SSIM和深度指标上显著优于所有基线（PSNR 20.42 vs 最佳19.04）。
- 扩散增强渐进推理（StereoSplat+）进一步将平均PSNR提升至20.76，SSIM 0.69，在遮挡区域和强外推场景（如鸟瞰图）性能提升更明显。
- 消融显示：双分支融合优于任一分支；随机子采样提升鲁棒性；扩散增强有效弥补伪视图的域间隙。
- 方法能够扩展到大规模场景：通过将长序列划分为bin并融合每个bin的3DGS，可实现增量重建。

## 7. 优点

- **创新性**：首次将前馈3DGS与一步扩散增强器结合实现从单立体对的因果重建，避免了测试时优化。
- **架构鲁棒性**：输入不变的随机子采样+连续位姿编码使模型适应不同视图数量与配置，实际部署灵活。
- **双分支互补**：代价体积分支提供精确的立体匹配，3D体积分支处理遮挡和弱纹理区域，优于纯代价体积或纯Transformer方案。
- **高效性**：仅执行一次渲染-增强-重新注入循环，保持前馈速度（未报告具体帧率，但理论实时）。
- **质量提升**：在PSNR、SSIM、深度指标上大幅领先现有前馈方法，尤其在挑战性区域。

## 8. 不足与局限

- **数据集局限性**：仅在KITTI-360（城市驾驶场景）上评估，未验证在室内场景、动态物体密集场景或不同传感器配置下的泛化能力。
- **依赖扩散模型质量**：一步扩散增强器可能引入新的伪影（如过度平滑或幻觉），且论文未详细分析失败案例。
- **单次渐进限制**：仅执行一轮渲染-增强-重新注入，若执行多轮可能累积误差，且论文未讨论多轮扩展的效果。
- **动态物体处理不足**：未显式建模动态物体（如移动车辆、行人），在大规模增量融合时可能导致时间不一致。
- **未与基于优化（非前馈）方法对比**：实验仅对比前馈方法，未与逐场景优化的3DGS方法（如原始3DGS或NeRF）对比，后者在相同输入下可能因过度拟合而表现更好，但牺牲速度。
- **深度监督依赖**：深度损失依赖稀疏LiDAR+伪密集深度，伪深度质量可能影响训练稳定性。
- **算力成本较高**：4块A6000训练30 epoch，虽有梯度检查点但资源需求较大。

（完）
