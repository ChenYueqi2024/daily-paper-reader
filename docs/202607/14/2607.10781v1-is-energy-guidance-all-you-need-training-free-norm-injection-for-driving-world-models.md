---
title: Is Energy Guidance All You Need? Training-Free Norm Injection for Driving World Models
title_zh: 能量引导是否足够？面向驾驶世界模型的无训练规范注入
authors: "Xiyan Su, Frank Diermeyer, Markus Lienkamp"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10781v1"
tags: ["query:d"]
score: 9.0
evidence: 驾驶世界模型与能量引导
tldr: 驾驶世界模型基于大视频扩散骨干生成逼真场景，但控制其遵守交通规范通常需要重训练或手建布局。本文针对联合生成未来视频与规划轨迹的rectified-flow模型，提出无需重训练的能量引导方法：在采样时通过可微能量函数注入驾驶规范，直接引导规划轨迹。在Open-Sora 2.0 MM-DiT上成功实现刹车引导，但发现生成视频未完全跟随引导轨迹，识别出跨流耦合不足是端到端可控的关键瓶颈。该工作首次证明无训练规范注入的可行性，并指明实现可控驾驶世界模型的关键需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10781v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10781v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 1124, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10781v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1849, \"height\": 655, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10781v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1837, \"height\": 2287, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10781v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1842, \"height\": 2254, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10781v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1547, \"height\": 1071, \"label\": \"Figure\"}]"
motivation: 无需重训练即可精确控制扩散世界模型遵守交通规范的方法。
method: 在采样时通过可微能量函数注入驾驶规范，引导rectified-flow模型的规划轨迹。
result: 在Open-Sora 2.0 MM-DiT上成功引导刹车，但生成视频未完全跟随，跨流耦合是瓶颈。
conclusion: 无训练能量引导可行，但端到端可控需增强跨流耦合。
---

## 摘要
基于大型视频扩散主干构建的驾驶世界模型能够生成逼真的场景，但难以控制：强制执行交通规范通常意味着重新训练主干或将其条件化于手工构建的布局。我们质疑控制是否真的需要训练。我们的实验表明，一个联合生成未来视频和规划自车轨迹的整流流驾驶世界模型，其规划轨迹可以在采样时完全由编码驾驶规范的可微分能量函数引导，而无需对扩散主干进行特定知识重训练。具体而言，我们证明基于Open-Sora 2.0 MM-DiT主干构建的世界模型可以通过在采样时注入能量引导来制动于反事实目标。然而，我们发现生成的视频尚未通过主干的联合自注意力遵循引导轨迹，并指出跨流耦合是端到端可控展开的关键要求。

## Abstract
Driving world models built on large video-diffusion backbones generate realistic scenes but are hard to control: enforcing a traffic norm typically means retraining the backbone or conditioning it on hand-built layouts. We ask whether controllability requires training at all. Our experiment shows that a rectified-flow driving world model, which jointly generates future video and a planned ego trajectory, can have its planned trajectory steered entirely at sampling time by differentiable energy functions that encode driving norms, without knowledge-specific retraining of the diffusion backbone. Concretely, we demonstrate that a world model built on Open-Sora 2.0 MM-DiT backbone can be steered to brake at a counterfactual target by injecting energy guidance at sampling time. However, we find that the generated video does not yet follow the steered trajectory through the backbone's joint self-attention and identify the cross-stream coupling as a crucial requirement for end-to-end-controllable rollouts.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于大型视频扩散模型（如Open-Sora 2.0）构建的驾驶世界模型能生成逼真场景，但控制模型遵守交通规范极其困难。传统方法要么需要在每次引入新规则时微调主干网络，要么需要在训练时加入手工构建的布局（如地图、边界框）作为条件。这些方式成本高、灵活性差，且无法在部署后于采样时动态调整或重新排序规则。
- **整体含义**：本文质疑是否必须通过训练来实现可控性，首次提出**无需重训练**的规范注入方法：在采样时利用可微能量函数直接引导生成的自车轨迹，并验证了这一范式的可行性。同时，揭示了当前架构中视频流与轨迹流耦合不足的关键障碍，为未来端到端可控驾驶世界模型指明方向。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将驾驶规范编码为可微能量函数 \(E(\hat{x}_0)\)，在采样过程中对rectified-flow模型的每个时间步预测的**干净自车轨迹估计** \(\hat{x}_0\) 进行能量梯度下降修正，再将修正后的轨迹转换回速度场，从而在完全冻结主干网络的情况下使生成轨迹满足规范。
- **关键技术细节**：
  1. **基础模型**：采用rectified-flow模型，其插值公式和速度场为：
     \[
     x_t = (1-t)x_0 + [\sigma_{\min} + (1-\sigma_{\min})t]x_1, \quad v_\theta(x_t,t) \approx (1-\sigma_{\min})x_1 - x_0
     \]
     其一阶干净样本估计为 \(\hat{x}_0 = x_t - t v_\theta\)。
  2. **能量引导步骤**（公式3）：
     \[
     \hat{x}_0' = \hat{x}_0 - \lambda(t)\nabla_{\hat{x}_0}E(\hat{x}_0), \quad v' = \frac{x_t - \hat{x}_0'}{\max(t,t_{\min})}
     \]
     其中 \(\lambda(t)\) 采用非对称调度（在 \(t\approx0\) 和 \(t\approx1\) 时为零），避免早期噪声大和后期 \(1/t\) 爆炸。
  3. **能量函数示例**：使用MSE损失将轨迹吸引到反事实减速目标（模拟前车急刹场景），权重可调。
  4. **稳定处理**：对每一步的修正幅度进行尺度无关裁剪，防止脱离数据流形。
- **算法流程**：采样时，每一步先通过主干得到预测速度 \(v_\theta\)，计算 \(\hat{x}_0\)；计算能量梯度并更新得到 \(\hat{x}_0'\)；反推修正速度 \(v'\)；以 \(v'\) 执行ODE积分。整个过程中主干网络不参与梯度计算（冻结），能量函数仅需一次轻量自动微分。

### 3. 实验设计：数据集 / 场景、基准（benchmark）、对比方法

- **数据集**：nuScenes v1.0-trainval（前摄像头，分辨率256×448，12帧片段，帧间隔12，非重叠）。
- **场景**：反事实减速场景——模拟前方有慢车，要求自车轨迹减速到指定目标位置。
- **基准与对比方法**：本文未与其他方法（如微调方法、条件生成方法）进行定量对比，主要进行内部对比：
  - **无引导（事实，factual）**：同一噪声种子下正常采样。
  - **有引导（反事实，counterfactual）**：加入刹车能量引导。
- **评估指标**：
  - **轨迹**：L2距离到减速目标（35个片段，均值±标准差）。
  - **视频**：逐帧光流差异（引导 vs 无引导），检查视频是否跟随轨迹变化。
- **无外部基准**：论文旨在概念验证，未与GAIA-1、DriveWM等世界模型比较，也未在通用视频生成指标（如FVD）上评测真实性。

### 4. 资源与算力

- **GPU型号**：单个NVIDIA HGX H100 94GB GPU。
- **训练时长**：约22,000步（约？小时，文中未明确时间）。
- **可训练参数**：88.1M参数（LoRA适配器+新引入模块），主干11B参数冻结。
- **优化配置**：AdamW，bf16，梯度激活检查点（gradient activation checkpointing），batch size 8。
- **采样开销**：能量引导仅需对小型解析函数求一次梯度，相较于主干前传可以忽略不计。

### 5. 实验数量与充分性

- **实验数量**：共35个可用采样片段（clip）的轨迹与视频分析；另外展示了单个片段的详细对比（Fig.4-6）。
- **消融**：未进行消融实验（例如不同 \(\lambda\) 值、不同能量权重、不同调度策略）。
- **对比**：无外部方法对比，仅有内部引导 vs 无引导。
- **充分性评估**：实验初步验证了轨迹可被引导，但视频未跟随。实验规模偏小（仅35个片段），且仅测试了一种能量（刹车），未测试多能量组合或真实驾驶规则（如限速、车道保持）。未评估生成视频的真实性（FVD等）和规则遵守率。因此实验设计尚不充分，缺乏系统性和定量benchmark。

### 6. 论文的主要结论与发现

- **主要发现**：
  1. **训练-free能量引导可有效控制自车轨迹**：在35个片段中，引导后轨迹末端距离减速目标的平均L2距离为1.3米，而无引导时达13.6米（Fig.3左）。
  2. **视频未能跟随引导轨迹**：引导前后视频的光流差异统计上为零（变化<2%，在片段间波动范围内），说明轨迹的引导未通过联合自注意力传播到视频生成（Fig.3右）。
- **结论**：无训练能量引导范式可行，但当前MM-DiT架构（仅采用双流块、跳过单流块）导致跨流耦合不足，是阻碍端到端可控的关键瓶颈。提出将视频流和轨迹流重新路由回单流块进行联合去噪作为潜在解决方案。

### 7. 优点：方法或实验设计上的亮点

- **创新性与简洁性**：首次在**rectified-flow驾驶世界模型**上实现**完全采样时、无重训练**的规范注入，方法简单且即插即用，不改变主干。
- **问题诊断清晰**：明确指出了视频不跟随轨迹的根本原因——跨流耦合不足，并分析了架构细节（单流块被绕过），为后续研究提供直接改进方向。
- **实验设计直观**：使用反事实减速场景（模型未训练过的目标）直接检验可控性，对比引导/无引导的轨迹和光流，结果清晰。
- **可复现与开源**：计划发布代码、模型权重（LoRA+新模块）、能量库，有助于学术界推进研究。

### 8. 不足与局限

- **实验覆盖有限**：仅在nuScenes单个数据集、单一场景（刹车）下测试，未验证其他驾驶规则（如车道保持、限速、避障），也未使用多能量组合。缺乏与其他可控世界模型（如GAIA-1、DriveWM）的定量对比。
- **缺乏客观指标**：未报告生成视频的真实性（FVD、IS等）或规则遵守率（如碰撞率、偏移量），仅有轨迹距离和光流差异，说服力不足。
- **方法本身局限性**：
  - 能量引导依赖一阶干净样本估计 \(\hat{x}_0\)，在采样早期和多重模态下估计不准确，导致梯度不可靠。
  - ODE采样缺乏随机自校正机制，易偏离数据流形，即使搭配调度和裁剪也无法完全避免。
- **视频控制失败**：核心目标（视频跟随轨迹）未达成，论文提供的解决方案（重新路由到单流块）仅是提议，未实验验证，整体贡献偏向“提出问题”而非“解决问题”。
- **消融与分析不足**：未深入分析不同能量强度、不同采样步数、不同引导时机对轨迹和视频的影响，也未量化跨流耦合强度。
- **资源描述不完整**：训练时间未明确给出（仅步数），也未报告采样时间或推理成本。

（完）
