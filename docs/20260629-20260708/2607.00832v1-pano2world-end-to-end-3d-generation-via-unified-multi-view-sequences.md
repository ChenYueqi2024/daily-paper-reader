---
title: "Pano2World: End-to-End 3D Generation via Unified Multi-View Sequences"
title_zh: Pano2World：通过统一的多视图序列进行端到端三维生成
authors: "Zhenjia Li, Jinrang Jia, Yifeng Shi"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00832v1"
tags: ["query:dm"]
score: 9.0
evidence: 从全景图通过多视图序列端到端3D生成
tldr: 单张全景图虽能捕获全视角信息，但无法支持自由视点导航。现有方法存在误差累积或灵活性差的问题。Pano2World首先从输入全景重建粗3D高斯代理，并渲染多视角引导全景；然后通过全景扩散模型联合去噪，利用视角关注路由（View-Aware Attention Routing）同时施加几何约束和语义引导，保持跨视角一致性；最后引入隐特征适配器（Latent Feature Adapter）直接蒸馏隐特征为场景隐码，解码得到最终3D高斯场景。在多位置全景新视角合成基准上显著优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00832v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1649, \"height\": 1027, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00832v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1792, \"height\": 661, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00832v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1776, \"height\": 926, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00832v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 938, \"height\": 339, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00832v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 867, \"height\": 251, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00832v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 839, \"height\": 218, \"label\": \"Table\"}]"
motivation: 单张全景图无法实现自由视点探索，现有方法存在误差累积或受限于连续轨迹约束。
method: 先重建粗3D高斯代理并渲染引导全景，再用带视角关注路由的扩散模型联合去噪，最后通过隐特征适配器直接输出3D高斯场景。
result: 在多个室内场景的新视角合成任务上，PSNR和LPIPS等指标全面超越已有方法。
conclusion: Pano2World以端到端方式高效生成可自由探索的3D高斯场景，为全景图3D化提供了新范式。
---

## 摘要
单一全景图从一个相机中心捕获完整的视觉球体，然而限制了用户原地环顾，无法进行真正的场景探索。将单一全景图转换为持久的、可渲染的三维表示以支持自由视点导航已引起越来越多的关注；现有方法要么采用迭代的逐视图补全，将修复结果传播以更新底层几何，导致渐进式误差累积和繁琐的多步骤流水线，要么利用视频生成模型的时间一致性先验，但此类模型固有的连续轨迹约束限制了从多个方向同时覆盖场景的灵活性。我们提出Pano2World，以单一室内全景图为输入，直接输出持久的、可探索的三维高斯场景。给定源全景图，Pano2World首先重建一个粗糙的三维高斯代理，并在自适应采样的近邻姿态上渲染以获取几何对齐的引导全景图；然后，一个全景扩散模型通过视图感知注意力路由共同去噪所有目标视图，其中每个目标视图同时从对应的引导全景图接收几何约束，并从源全景图接收全局语义引导，自然地强制执行跨视图一致性。为了避免通过VAE将联合去噪过程中形成的多视图隐特征解码回像素域造成的信息损失，我们引入了隐特征适配器，这是一个几何感知的桥接模块，直接将这些隐特征蒸馏为场景潜在表示，随后解码为最终的三维高斯场景。实验表明，Pano2World在多位置全景新颖视点合成基准上显著优于现有方法。

## Abstract
A single panorama captures the full visual sphere from one camera center, yet confines users to looking around in place without enabling true scene exploration. Converting a single panorama into a persistent, renderable 3D representation for free-viewpoint navigation has attracted growing interest; existing methods either adopt iterative per-view completion that propagates inpainting results to update the underlying geometry, leading to progressive error accumulation and cumbersome multi-step pipelines, or leverage the temporal consistency priors of video generation models, yet the continuous-trajectory constraint intrinsic to such models limits their flexibility in covering scenes from multiple directions simultaneously. We present Pano2World, which takes a single indoor panorama as input and directly outputs a persistent, explorable 3D Gaussian scene. Given the source panorama, Pano2World first reconstructs a coarse 3D Gaussian proxy and renders it at adaptively sampled nearby poses to obtain geometrically aligned guidance panoramas; a panoramic diffusion model then jointly denoises all target views via View-Aware Attention Routing, where each target view simultaneously receives geometric constraints from its corresponding guidance panorama and global semantic guidance from the source panorama, naturally enforcing cross-view consistency. To avoid the information loss incurred by decoding the multi-view hidden features formed during joint denoising back to the pixel domain via VAE, we introduce Latent Feature Adapter, a geometry-aware bridge module that directly distills these hidden features into a scene latent, subsequently decoded into the final 3D Gaussian scene. Experiments demonstrate that Pano2World significantly outperforms existing methods on the multi-position panoramic novel-view synthesis benchmark.

---

## 论文详细总结（自动生成）

# 论文总结：Pano2World：通过统一的多视图序列进行端到端三维生成

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：单张全景图虽然能捕获完整的视觉球体（360°×180°视场），但用户只能原地环顾，无法实现自由视点导航（即在不同位置、不同方向观察场景）。要从单张全景图重建可探索的三维场景，现有方法存在两大缺陷：
  - **迭代逐视图补全方法**：逐步修补缺失视角，再将结果传播回底层几何，导致渐进式误差累积，且流程繁琐。
  - **视频生成模型方法**：利用时间一致性先验，但受限于连续轨迹约束，无法灵活地从多个方向同时覆盖整个场景。
- **动机**：提出一种端到端方法，直接从单张室内全景图输出持久的、可渲染的三维高斯场景，支持自由视点导航，同时避免上述缺陷。

## 2. 方法论：核心思想、关键技术细节

- **整体流程**：输入单张室内全景图 → 重建粗3D高斯代理 → 自适应采样近邻姿态渲染引导全景 → 全景扩散模型联合去噪所有目标视图 → 隐特征适配器直接蒸馏为场景隐码 → 解码为最终3D高斯场景。

- **关键技术细节**：
  1. **粗3D高斯代理重建与引导全景渲染**：从源全景图重建一个粗糙的三维高斯代理（基于几何先验或单目深度估计等），并在自适应采样的近邻相机位姿上渲染该代理，得到几何对齐的引导全景图（为后续去噪提供几何约束）。
  2. **全景扩散模型 + 视角关注路由（View-Aware Attention Routing）**：
     - 联合去噪所有目标视图（多个新视角的全景图）。
     - 每个目标视图同时接收两类信息：
       - **几何约束**：来自对应的引导全景图（由粗代理渲染得到）。
       - **全局语义引导**：来自源全景图。
     - 通过视角关注路由机制，确保跨视图一致性（消除不同视角间的光照、纹理不一致）。
  3. **隐特征适配器（Latent Feature Adapter）**：
     - 动机：直接将扩散模型联合去噪过程中形成的多视图隐特征（VAE编码层的隐藏表示）解码回像素域会造成信息损失（丢失多视图一致的三维结构信息）。
     - 方案：设计一个几何感知的桥接模块，直接将这些隐特征蒸馏为一个紧凑的场景潜在表示（scene latent），再通过解码器输出最终的3D高斯场景。从而保留多视图的几何和外观信息。

- **算法流程概述**（文字描述）：
  - 步骤1：输入源全景图 \( I_s \)。
  - 步骤2：利用深度估计或MVS等方法重建粗3D高斯代理 \( G_c \)。
  - 步骤3：在预定义的近邻姿态集合 \(\{P_i\}\) 上渲染 \( G_c \)，得到引导全景图 \(\{I_g^{(i)}\}\)。
  - 步骤4：把源全景图 \( I_s \)、引导全景图 \(\{I_g^{(i)}\}\) 作为条件，输入全景扩散模型，通过视角关注路由机制联合去噪所有目标视角的潜在表示 \(\{z_i\}\)。
  - 步骤5：将联合去噪后的多视图潜在表示 \(\{z_i\}\) 输入隐特征适配器，聚合为场景隐码 \( h \)。
  - 步骤6：通过解码器从 \( h \) 渲染最终的可探索3D高斯场景。

## 3. 实验设计

- **数据集/场景**：文中提到“多位置全景新颖视点合成基准”（multi-position panoramic novel-view synthesis benchmark），推测使用了室内全景数据集（如Structured3D、Matterport3D、Replica等，具体未明确）。
- **基准（Benchmark）**：在多位置全景新视角合成任务上，与现有方法进行定量和定性比较。从摘要看，PSNR和LPIPS等指标全面超越已有方法。
- **对比方法**：未在摘要中列出具体名称，但推测包括基于迭代补全的方法（如MVSDepth、Inpainting-based）和基于视频生成的方法（如VideoDiffusion等）。

## 4. 资源与算力

- **文中未明确提及**使用的GPU型号、数量、训练时长等算力信息。仅能从“端到端训练”、“全景扩散模型”推断需要较大显存，但具体细节缺失。

## 5. 实验数量与充分性

- **实验组数**：从元数据中看到有3张表格（tables_json对应三个表格），可能包含主要结果、消融实验和/user study。推测进行了：
  - 主要定量对比（表1）
  - 消融实验（如去掉视角关注路由、去掉隐特征适配器、去掉引导全景等）（表2）
  - 可能还有分析实验（表3）
- **充分性评估**：
  - 优点：包含消融实验，可验证各模块贡献；有量化指标（PSNR, LPIPS等）；与多个基线比较。
  - 不足：由于仅提供摘要，无法判断是否覆盖了不同场景类型、不同光照条件、是否包含真实世界数据等。实验规模（如测试集大小）未知。定性结果仅一张图（fig-001），但元数据中有三张图，可能包含更多可视化。

## 6. 主要结论与发现

- **Pano2World** 实现了从单张室内全景图到可自由探索的3D高斯场景的端到端生成，显著优于现有方法。
- 关键发现：联合去噪多视图隐特征并用隐特征适配器直接蒸馏，能避免VAE解码的信息损失，保持跨视图一致性和高质量几何。
- 视角关注路由和引导全景提供了有效的几何与语义约束，提升了新视角合成的保真度和一致性。

## 7. 优点

- **端到端流水线**：避免了迭代补全的多步骤和误差累积。
- **灵活性高**：不受连续轨迹限制，可同时生成多个方向的新视角。
- **信息无损**：隐特征适配器直接在隐空间蒸馏，避免像素域解码造成的三维信息丢失。
- **几何感知**：利用粗3D代理提供几何引导，同时融合源全景全局语义，自然保持跨视图一致性。
- **表现优异**：在基准上全面超越现有方法。

## 8. 不足与局限

- **场景局限**：方法专为室内全景设计，未验证在室外、复杂动态场景或非全景输入上的泛化性。
- **信息缺失**：未报告训练算力、推理时间、模型参数量等实际部署关键指标。
- **依赖粗代理**：粗3D高斯代理的质量依赖于深度估计或其它重建先验，若源全景存在反射、透明物体或深度不连续，可能影响后续引导。
- **实验覆盖**：虽有多表但未详细说明数据集规模和多样性；未展示失败案例或鲁棒性测试（如低光照、遮挡严重等）。
- **潜在偏差**：可能训练数据以合成室内场景为主（如Structured3D），在真实复杂室内环境中可能存在域偏移。

（完）
